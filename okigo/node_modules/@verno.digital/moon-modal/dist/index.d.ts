import { Close, Info, methodOptions, modalOptions, modalType, Open } from './types/types';
declare class MoonModal {
    #private;
    constructor(options: modalOptions);
    open(el: modalType, options?: methodOptions): Open;
    close(): Close;
    set timeout(value: number);
    get info(): Info;
}
export { MoonModal };
